import { IfDataToggleDirective } from './if-data-toggle.directive';

describe('IfDataToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfDataToggleDirective(null);
    expect(directive).toBeTruthy();
  });
});
